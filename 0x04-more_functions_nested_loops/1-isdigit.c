#include "main.h"
/**
 * _isdigit - function that checks a digit 0 - 9
 * @c: input
 * Return: 1 if c is digit an d 0 otherweise
 */
int _isdigit(int c)
	{
		((c >= 48) && (c <= 57))

	return (1);
		else
			return (0);
	}
